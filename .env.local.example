# 複製這個檔案為 .env.local 並填入實際值即可啟用（.env.local 不會進版控）
#
# airaTracker 相簿上傳（選用功能）
# 設定 VUE_APP_TRACKER_UPLOAD_URL 之後，自助報到（SelfCheckinDashboard）
# 在訪客資料更新成功時，會把該張照片一併上傳到 airaTracker 相簿。
# 未設定 URL 時整個上傳流程會直接跳過，不影響報到。
#
# 注意：目標主機需允許跨域，且要放行自訂 header sessionId 的 preflight；
# 若使用自簽憑證，機器上要先手動信任該憑證。

VUE_APP_TRACKER_UPLOAD_URL=https://TRACKER_HOST:8443/airaTracker/albums/uploadPhoto
VUE_APP_TRACKER_SESSION_ID=
VUE_APP_TRACKER_ALBUM_ID=
